pre-commit:
  commands:
    check:format:
      glob: '*.{js,ts,md}'
      run: pnpm run check:format
    check:filename:
      glob: '*.{js,ts}'
      run: pnpm run check:filename
    lint:
      glob: '*.{js,ts,md}'
      run: pnpm run lint
